const INITIAL_STATE = {
  loading: false,
  error: false,
  colors: []
}

export default (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state
  }
}
